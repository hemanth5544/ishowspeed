{
  "name": "ishowspeed",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:frontend": "turbo run dev --filter=client",
    "build": "turbo run build",
    "build:frontend": "turbo run build --filter=client",
    "start:frontend": "turbo run start --filter=client"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "next-themes": "^0.4.6"
  }
}
